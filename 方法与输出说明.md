# 方法与输出说明

版本：0.1.1-mac-development。Mac 安装步骤见 00_Mac开始这里.html。

## 3. 转换输出是什么

每个运行批次创建独立目录，每张照片创建与原文件哈希关联的子目录。

- `RGB_linear16.tif`：可选输出，三个通道均为16位整数；采用sRGB原色的线性光编码，没有附加非线性sRGB曲线，也没有嵌入经标定的ICC配置。
- `Y_linear32.tif`：正式用于本包差分计算的单通道32位浮点图。输出16位RGB先除以固定65535，再按固定系数计算Y，数值使用统一的归一化编码尺度。
- `preview_DISPLAY_ONLY.png`：缩小后的显示副本，使用统一的显示转换；不用于数值分析。
- `metadata.json`：原文件哈希、拍摄元数据、固定参数、软件版本、传感器黑白电平记录、元数据比较和输出哈希。
- 批次根目录：`fixed_profile.json`、`runtime_environment.json`、`batch_summary.csv` 和 `batch_summary.json`。

RAW保持只读，绝不覆盖。转换后的TIFF会重新读入检查位深、形状和像素一致性。

### 元数据检查规则

与参考RAW比较已记录的相机、镜头、ISO、曝光时间、光圈、焦距和方向。发现明确差异时，该文件报错，不通过自动调亮来掩盖差异。

缺失字段单独列为“未核查”。软件检查通过的最多是“已记录字段相同”，实际照明、板面角度、对焦和取放位置仍需现场检查。

批次状态始终标记 `DEVELOPMENT_NOT_VALIDATED`。代码尚未提供“正式方法已验证”开关。

## 4. 正式照片接入 Fiji 时的规则

只把按同一处理流程导出的 `Y_linear32.tif` 用于本包宏。不要使用PNG预览，不要先转8位，不要对单张图独立拉直方图。

宏不自动配准。正式照片取放后，必须先根据预定标记或稳定背景检查对齐；无法恢复相同拍摄角度、尺度和照明时，应重新拍摄。自动图像配准也无法消除反射几何变化。

分析ROI是事前规定的矩形，在同一组里保持相同物理覆盖范围。擦拭后应覆盖完整轨迹和必要边缘，不要只圈最初的圆形手印。

本版主计算为：

`D = abs(Y_state - Y_clean)`

`V = mean(D[fixed_ROI])`

Y与V是图像编码量，尚未经验证为实际人眼可见度、L*、光学亮度、残留质量或去污率。单张图里的像素数不等于独立试验次数。

本版不计算印迹面积，不设置阈值，不产生综合权重或合格等级。保留全幅差分图用于检查迁移、拖痕、灰尘和擦亮等。

显示时固定全组显示范围。可以调整“显示窗口”便于查看，但不要使用Normalize、直方图均衡等改写测量像素。截图不能作为分析输入。

## 5. 哪些事情现在做，哪些等到货

现在可做：安装环境；打开GUI；准备文件命名；用旧RAW核查读取和输出；用本包合成TIFF练习ROI和差分；确认记录文件结构；保存依赖版本。

到货后确定：固定照明；正式白平衡系数；曝光及饱和边界；统一测区；标记和取放误差；实际噪声水平；质量检查阈值；压印与擦拭时间；与真实观感的对应关系。

建议原文件名事先包含：样品编号、独立印迹编号、状态和时间点，例如 `PU01_R01_clean.ARW`、`PU01_R01_print_t030.ARW`、`PU01_R01_wipe1_t030.ARW`。本版不按拍摄顺序猜测状态；相互比较的文件由操作者明确选定。


## 版本和架构

本包的 Apple 芯片安装分支固定 rawpy 0.27.1，Intel 分支固定 rawpy 0.25.1；其余依赖固定相同版本。两套解码环境不能默认像素等效。配置记录并核对 rawpy、LibRaw、NumPy、tifffile、系统和进程架构。旧 V0.1 配置需在本机重新建立，避免跨环境混用。

在相同 RAW 上重复转换时可核对输出数组；记录中有时间戳，不能要求所有 JSON 文件哈希一致。
